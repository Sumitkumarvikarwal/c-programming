#include<stdio.h>
#include<string.h>
int main()
{
    char name[]= "sumit";
    // int length = strlen(name);
    // printf("length is :%d\n",length);
    //0r
    printf("length is :%u\n",strlen(name));
return 0;
}